#include <bits/stdc++.h>
#include <ncurses.h>
#include <pthread.h>
#define endl '\n'

using namespace std;

int main(int argc, char *argv[]) {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    cout << "Hello World!" << endl;

    return 0;
}
